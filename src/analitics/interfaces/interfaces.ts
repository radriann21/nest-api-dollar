export enum GapTrend {
  STABLE,
  INCREASING,
  DECREASING,
}

export enum Periods {
  YESTERDAY = 'yesterday',
  WEEK = 'week',
  MONTH = 'month',
}

export enum VolatilitySource {
  Binance = 'Binance',
  BCV = 'BCV',
}
